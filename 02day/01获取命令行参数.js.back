//接受两个参数
var args=process.argv.slice(2)

switch (args[0]) {
    case 'install':
        console.log(`正在安装${args[1]}`)
        break
    case 'uninstall':
        console.log(`正在卸载${args[1]}`)
        break
}